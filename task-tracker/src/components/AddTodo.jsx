import { useEffect, useState } from 'react';

const AdicionarTodo = ({ todo }) => {
if(props.open[0] != 0){
    return(
        <div>
            <p>{todo.text}</p>
            <button>Adicionar Novo Todo</button>
        </div>
        )
    }

}
export default AdicionarTodo;